variables:
  USE_SQLITE: 1
  POETRY_VIRTUALENVS_CREATE: "false"  # This is to ensure poetry uses the system Python

default:
  image: python:3.1

#  cache:
#    paths:
#      - ~/.cache/pip/

  before_script:
    - rm -rf "%CACHE_PATH%/%CI_PIPELINE_ID%"
    - add-apt-repository ppa:deadsnakes/ppa -y
    - apt-get -yq update
    - apt-get -yq install python3.12 python3-pip
    - python3.12 -m pip install -U pip setuptools
    - python3.12 -m pip install --upgrade poetry
    - poetry install

migrations:
  stage: build
  script:
    - poetry run python3 manage.py migrate


django-tests:
  stage: test
  script:
    - poetry run pytest



